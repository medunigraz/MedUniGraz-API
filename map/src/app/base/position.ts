export class Position {
  public x: number;
  public y: number;
  public level: number;

  constructor(x: number, y: number, level: number) {
    this.x = x;
    this.y = y;
    this.level = level;
  }
}
