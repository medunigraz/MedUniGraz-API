import {SearchResult} from '../base/searchresult';

export const DefaultStartPointWithPos: SearchResult = new SearchResult(0, "Aktueller Standort", 1);
export const DefaultStartPoint: SearchResult = new SearchResult(0, "Haupteingang", 1);
